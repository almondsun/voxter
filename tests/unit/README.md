# Unit Tests

Unit tests belong here.

Prioritize deterministic logic:

- timestamp and label alignment
- frame-stack indexing
- preprocessing shape and dtype contracts
- binary action transition extraction
- press/release timing metrics
- threshold and hysteresis behavior
- dataset split validation

Keep unit tests independent of the live game window, physical input APIs, and
large local datasets.
