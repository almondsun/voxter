# Assets

Static project assets live here. Assets should be versioned only when they are
small enough for normal Git history and are required to reproduce repository
behavior, examples, or tests.

Large generated captures, model checkpoints, and derived datasets do not belong
in this tree. Put those under the appropriate `data/` subdirectory or external
artifact storage.
